# TechCorp Onboarding RAG Portal — Python Dependencies
# Install with: pip install -r requirements.txt

# Web framework
flask>=3.0.0

# AWS SDK — used for S3 (aws_setup.py) and Bedrock (app.py)
boto3>=1.34.0

# Loads credentials from the .env file at startup
python-dotenv>=1.0.0
